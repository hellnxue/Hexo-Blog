sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - master # build master branch only
script:
  - hexo generate # generate static files
deploy:
  provider: pages
  skip-cleanup: true
  github-token: 7cd457fc2108ce2c76c3957e6317983ff99a6511  #$GH_TOKEN
  keep-history: true
  on:
    branch: gh-pages
  local-dir: public